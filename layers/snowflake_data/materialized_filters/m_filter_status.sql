
 

select INITCAP(is_currently_ignored) as is_currently_ignored from "SANDBOX"."TC_TEST"."CURRENTLY_IGNORED" 
--has the same values as is_currently_present


{{ column(
 name='IS_CURRENTLY_IGNORED',
 tags=['ids','names']
)}} 
